# Notas : 

Primer programa en Node

```
Options:
      --help       Show help                                           [boolean]
      --version    Show version number                                 [boolean]
  -b, --base[number] [required] [default: Es la base de la tabla de multiplicar]
  -l, --listar                  [boolean] [default: Muestra la tabla en consola]
  -h, --iteracion                      [number] [default: Numero de iteraciones]

  ```